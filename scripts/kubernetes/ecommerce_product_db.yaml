apiVersion: v1
kind: Service
metadata:
  name: product-db-svc 
spec:
  clusterIP: None  # Ini headless service
  selector:
    app: product-db-svc
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: product-db-svc
spec:
  serviceName: product-db-svc
  replicas: 1
  selector:
    matchLabels:
      app: product-db-svc
  template:
    metadata:
      labels:
        app: product-db-svc
    spec:
      containers:
        - name: product-db-svc
          image: ghcr.io/sony-nurdianto/product_db_svc:v0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: product-db-svc
              protocol: TCP
          env:
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          securityContext:
            privileged: false
      imagePullSecrets:
        - name: github-secrets

